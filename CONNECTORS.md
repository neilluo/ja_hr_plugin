# Connectors

本套件通过**品类占位符**引用外部工具；实际使用时，对应品类连上任意一个工具即可。数据底座默认走钉钉 AI 表格。

| 品类 | 连上后能做什么 | 支持的工具 | 配置方式 |
|------|---------------|-----------|----------|
| 数据表格 | 岗位/简历/匹配全部批量落库、查重、定向匹配、脚本重算分数与岗位统计、看板取数、写后回读 | 钉钉 AI 表格 | 千问办公 → 设置 → 连接器 → 开启并授权「钉钉」即可（内置，套件默认底座） |
| 邮箱（可选） | 把收到的简历邮件附件直接纳入入库流程 | 钉钉邮箱 / Microsoft 365 | 在连接器设置开启 |
| HR 系统（可选增强） | 拉取外部 ATS 在招岗位、回写 offer 状态 | Moka HR / 薪人薪事 / 钉钉假勤 | 连接器市场启用对应服务后，可扩展新技能接入 |

> 所有技能在没有连接器时仍可产出结构化草案/建议清单；连接钉钉 AI 表格后升级为完整批量落库与定向匹配。数据底座为**内置钉钉连接器（已连接）**，因此本套件不含 `.mcp.json` 引导配置项。

## 本机运行时前提（不是连接器，但必须有）

极速版的解析/批量落库/评分重算由自带 Python 脚本完成（零第三方 pip 依赖，olefile/pypdf 已 vendor）：

- **机器上必须存在 Python 3（3.9+）**。千问办公不自带 python 运行时，缺失时需先安装。
- macOS / Linux 用 `python3`；Windows 用 `py -3`（裸 `python` 别名可能静默失败，退出码 49）。
- `dws` CLI 已登录（脚本内部通过它批量读写 AI 表格）。
- 两条命令自检，**分开执行、不要用 `&&` 串**（PowerShell 5.1 不认 `&&`，Windows 用户粘进去直接报「标记"&&"不是此版本中的有效语句分隔符」）：① `python3 -c "import sys;assert sys.version_info[:2]>=(3,9),sys.version;print(sys.version)"` 查版本界（要求 Python **3.9+**，低于 3.9 当场抛 AssertionError，不会拖到 import 才炸）；② `dws aitable base list --limit 1` 查登录态。Windows 把 `python3` 换成 `py -3`，**不要用裸 `python`**（可能是 Microsoft Store 别名，静默失败、退出码 49）。

## 组织口径提醒

连接器授权哪个组织，读写的就是哪个组织的 AI 表格。多组织时确认授权组织与「招聘筛选」表所在组织一致；出现"查不到数据/匹配不到"时，先检查当前登录组织是否漂移。多公司/一部门一表场景：各自机器上各自生成本机的 `config.json`（脚本唯一 ID 源，由「复刻部署」技能接线生成，不进版本库），互不污染。
